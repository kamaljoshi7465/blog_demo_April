<div>
  <h1><%= @article.title %></h1>
  <p><%= @article.body %></p>
</div>
<%= link_to "Edit", edit_article_path %>
<%= link_to "Destroy", article_path(@article), 
data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  }
%>
